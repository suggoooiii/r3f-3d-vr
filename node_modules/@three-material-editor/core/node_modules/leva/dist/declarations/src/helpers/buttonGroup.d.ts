import type { ButtonGroupInput } from '../types';
/**
 *
 * @param name button name
 * @param onClick function that executes when the button is clicked
 */
export declare function buttonGroup(opts: {
    [title: string]: () => void;
}): ButtonGroupInput;
