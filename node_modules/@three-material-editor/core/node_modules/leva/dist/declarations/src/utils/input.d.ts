import { DataInput } from '../types';
/**
 * This function is used to normalize the way an input is stored in the store.
 * Returns a value in the form of { type, value, settings} by doing different
 * checks depending on the input structure.
 *
 * @param input
 * @param path
 */
export declare function normalizeInput(input: any, path: string): any;
export declare function updateInput(input: DataInput, newValue: any): void;
declare type SanitizeProps = {
    type: string;
    value: any;
    settings: object | undefined;
};
export declare function sanitizeValue({ type, value, settings }: SanitizeProps, newValue: any): any;
export {};
