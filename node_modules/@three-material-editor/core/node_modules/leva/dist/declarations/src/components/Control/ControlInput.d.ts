/// <reference types="react" />
declare type ControlInputProps<V, Settings extends object> = {
    type: string;
    label: string;
    valueKey: string;
    path: string;
    value: V;
    settings: Settings;
    disabled: boolean;
    setValue: (value: any) => void;
    setSettings: (settings: any) => void;
    optional: boolean;
    hint?: string;
    disable: (flag: boolean) => void;
};
export declare function ControlInput<V, Settings extends object>({ type, label, path, valueKey, value, settings, setValue, disabled, ...rest }: ControlInputProps<V, Settings>): JSX.Element | null;
export {};
